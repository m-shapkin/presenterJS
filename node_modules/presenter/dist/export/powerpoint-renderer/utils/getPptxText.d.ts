import { default as pptxgen } from 'pptxgenjs';
import { TextStyle } from '../../../types/TextStyle';
import { TextUnit } from '../../../types/TextUnit';
interface Args {
    readonly length: number | null;
    readonly opacity: number;
    readonly pixelsPerInch: number;
    readonly scale: number;
    readonly style: TextStyle;
    readonly textUnits: readonly TextUnit[][];
}
export declare function getPptxText({ length, opacity, pixelsPerInch, scale, style, textUnits, }: Args): pptxgen.TextProps[];
export {};
